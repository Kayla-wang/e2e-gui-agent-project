# 《小红书 GUI Agent 在智能化测试中的工程落地实践》读后笔记

> 来源：冬司（张亦驰）· 小红书 GUI Agent 方向负责人 · QCon 北京 2026 · 49 页
> 副标题：**像做 AI Coding 一样做自动化测试**
> 记录日期：2026-08-04

---

## 一、一句话总览

不要做一个"会点手机的通用 Agent"，而是**给 Coding Agent 套一层 GUI 的 Harness**：
用自然语言写测试意图 → LLM 探索一次并生成确定性代码 → 代码固化进 CI 做零 Token 回归 →
执行中沉淀的经验回流进分层知识库和页面路径图谱，下次生成得更准。

三个支点：**Code-as-Action**（而不是 ToolCall）、**脚本固化**（AI 只在生成/自愈时介入）、
**分层知识库 + 路径图谱**（把"业务理解"这件事变成可版本控制的仓库资产）。

一条贯穿全篇的组织原则：**人做决策和定标准，Agent 做执行和探索。**

---

## 二、核心要点提炼

### 2.1 痛点：传统 UI 自动化为什么不算真提效（p6–9）

- 四重结构性困境：**时间成本高**（抓坐标 → 写 XPath → 调试，一个点击半小时起）、
  **跨端成本 ×3**（iOS / Android / 鸿蒙 三套脚本）、**维护成本失控**（XPath 静态、版本每周一变）、
  **断言能力弱**（只能判元素存在性，不理解语义和视觉）。
- 关键洞察不是"脚本难写"，而是**静态选择器与高频迭代之间是结构性矛盾**——投入再多也追不上。
- 测试投入时间分布：执行用例 50%、撰写用例 20%、方案与环境 15%、横向沟通 15%。
  → 传统自动化只打了那 50% 里的一部分，**方案 + 用例生成的 35% 完全没被覆盖**。
- 自然语言用例 4 行 vs Python 脚本 10+ 行硬编码——表达力差距是这套方案的起点。

### 2.2 演进三阶段（p10–13）

| 阶段 | 时间 | 代表 | 解决什么 | 局限 |
|---|---|---|---|---|
| Stage 1 传统自动化 | < 2024-02 | Maestro | 稳定用例固化回放 | 纯工程脚本，维护更新困难 |
| Stage 2 AI 增强提效 | 2025-06 | Midscene.js | 原子动作泛化、解耦设备 | 用例生成与维护仍需大量人工 |
| Stage 3 自主执行 | 2025-10 | 自研 GUI Robot | Agent 自主探索 + 自我学习 | 本文主体 |

触发 Stage 2 的是"视觉模型足够强"，触发 Stage 3 的是"Coding Agent 足够强"。
**能力跃迁由模型驱动，但架构是自己设计的**——这是全篇最值得抄的思路。

### 2.3 边界定位：不追求 100%（p16）

- 70–80% 可自动化：功能回归、流程验证、多机型兼容、接口联动、DSL 验证 → 模型容易解决。
- 15–20% 可辅助自动化：偶现问题、复杂逻辑组合 bug、多端联动（下单→发货→物流）。
- 5–10% 短期无法自动化：极端路径、暴力回归、弱网/低电量、视觉审美判断。
- **最关键的一句**：这 5–10% 的非常规用例占"用例数"极少数，却占 **bug 数的大多数**。
- 策略：**达到 80% 的普通测试水准，而不是取代最顶尖的测试专家。**

### 2.4 三层架构（p18）

| 层 | 谁写 / 何时跑 | 约束 |
|---|---|---|
| 业务意图层 | 人类编写 · 版本控制 | 前置 review，意图确认前置。人类**唯一**需要深度参与的部分 |
| Agent 探索层 | LLM 实时执行 · 按需触发 | **不允许修改用例意图**，只允许泛化 |
| 可执行代码层 | LLM 生成 · CI 回归 | 确定性 > 95%，只允许局部纠偏，"零" Token |

这三条约束是整套方案的安全带：**灵活性放在中间层，确定性放在两端。**

### 2.5 六个关键问题 → 解法（p17）

脚本执行侧：
1. 执行过程泛化 → Code-as-Action + 自然语言 SDK
2. 模型执行成本高 → 脚本固化 · 零 Token 回归 · 分离视觉执行器
3. 灵活性 vs 确定性 → 三层架构分离关注点，确定性前置

上下文管理侧：
4. 需求理解困难、窗口有限 → 分层知识库 · 按需检索
5. 经验性测试、暴力边界用例 → 自我学习 · 知识沉淀
6. 模型迷失/跑偏 → 路径引导 · 操作图谱

### 2.6 执行引擎（p21–32）

- **模型分工是成本设计的核心**：主 Agent 用 GPT / Sonnet 级（长上下文、编码、泛化），
  视觉理解单独拆成小上下文的 gemini-flash 级 Agent（视觉断言 + 视觉定位）。
  贵模型负责思考，便宜模型负责看图。
- **Skill 仓库**是能力载体：计划生成、环境准备、用例生成、错误检测、报告生成，
  下挂 diff 影响面 / 造数 / Mock / 终端控制 / 视觉理解。开发时先放 repo，后续抽离成独立 Skill 包。
- 元素定位是**三层兜底**：语义理解 → DOM 结构 → 视觉识别，用来抹平端差异；
  统一驱动 API 封装 Android / iOS / 鸿蒙 的 Driver 差异。
- 视觉断言 skill 除了断言，还承担**指令预处理**：动态识别弹窗和业务分支，实时更新指令序列。
- 环境准备被明确标注"**最简单又最难的坑**"：登录、跳转、埋点、实验 Mock、Switch Mock、
  抓包 Mock、环境切换、模拟定位、RN 包、DSL 配置、手势轨迹、降级、内测……
- **纯视觉模型的三个局限**（p27–28）：业务不理解、精度与智能不够、受预训练数据影响。
  典型 case：页面标题是"竹子"、图里画的是玉簪植物，模型据图判断断言失败——
  这说明**断言的语义边界必须由人在意图层写清楚**，不能指望模型自己猜"该看标题还是看图"。
- **Code-as-Action vs ToolCall**（p29）：可固化性、调试能力、表达力、执行成本四项 Code 胜，
  只有**遵循性** ToolCall 胜（ToolCall 训进了模型，Code 依赖模型的 coding 能力）。
- 执行闭环（p30）：原始用例 → 自然语言测试计划 → 自然语言用例描述 → LLM 生成调用代码 →
  执行；失败则**错误分类诊断**分流为环境错误（环境修复）/ 执行错误（自愈重试）/
  用例错误（反馈修正）；成功则**脚本固化** → CI 确定性回归（零 Token）→ 报告。
- 固化产物是普通 shell / node / python 脚本（p31），坐标已从图谱命中、置信度 100%，
  断言降级为 OCR contains——**推理阶段的智能被编译成了确定性的断言**。

### 2.7 知识库与主动学习（p34–42）

- 四类挑战 → 四种解法：钻死胡同 → 路径引导；信息分散 → 一个大仓；
  难沉淀 → 自动沉淀；内容过载 → **主动清理**（记忆太少重复踩坑，太多产生幻觉）。
- 分级上下文四层：**跨项目**（经验级·通用：测试方法论、常见缺陷模式、造数经验、行业最佳实践）
  → **跨需求**（产品级·专属：业务框架、页面功能概要、历史测试记录、动线路径引导）
  → **单需求**（需求级·本次：PRD、代码 Diff、接口协议、设计稿）
  → **碎片记忆**（长期记忆：踩坑经验、学习的技巧，Agent 自主维护、无版本控制）。
  前三层 Agent 维护 + 人选择性审核、跟随代码版本；第四层完全自治。
- 知识库走 **Git 分支模型**（p36）：master 存产品级 + 经验级；拉需求分支建需求级文件夹；
  研发过程中只更新需求级；**需求上线时才把需求级合并回产品级 & 经验级**。
  这是"知识不会被单次测试污染"的机制保证。
- 目录分层标准：**L0 摘要层**（200 行上限，帮 Agent 判断要不要深入，自动淘汰过期内容）
  → **L1 概览层**（2000 行上限，提供层级结构作为路由）→ **L2 详情层**（按需逐层检索、置信度过滤）。
  每个目录一个 `Agents.md` 描述本目录摘要与子目录结构。
- 路径图谱四个字段的使用规则（p42）：
  `recalledPaths` 图谱已有精确路径（优先用，每个 node 带截图和操作）；
  `suggestedPaths` AI 推导路径（次选，步骤标 ⚠ 待验证）；
  `missingKnowledge` 图谱空白（在用例里标注并反馈给维护同学）；
  `confidence.overall` ≥80 直接用 / 60–79 注明"建议执行后校验" / <60 加警告要人工 review。
- 图谱数据来源三路：历史 case 执行记录、真实流量、人工录入；执行结束自动更新到**变更暂存区**，
  **周 Review 后合并**——写入不是即时的，这是防污染的第二道闸。

### 2.8 数据（p14 / p42 / p44）

| 指标 | 数值 |
|---|---|
| 自动化率 | 75% |
| 设备 × 场景组合 | 106 × 428 |
| 兼容性 Bug 自动召回率 | 68% |
| 累计自动化执行 | 12.3w+ 用例/次 |
| 用例采纳率 | 92% |
| 需求覆盖率 / 用例渗透率 | 66% / 63% |
| S0 核心 Case 常态化运行 | 600+ |
| 存量路径召回 / 增量召回 / 高置信度占比 | 100% / 75% / 90% |
| 存量需求路径覆盖 | 95% |
| 用例执行稳定性 | 98% |
| 单用例执行成本 | 1 $ |
| 回归推理 Token | 0 |

### 2.9 三个坑（p46）

1. 让人做决策和制定标准，让 Agent 做执行和探索；**不要一开始就让 Agent 独自端到端解决所有事**。
2. 重点不是打造通用 Agent，而是**基于 Coding Agent 做 GUI 的 Harness**。
3. 好的 Agent 不是评测集里迭代出来的，提示词和上下文都是真实场景跑出来的；
   **不要为短期指标做工程硬编码**。

---

## 三、读后笔记：我认为真正有价值的几个判断

### 3.1 "固化"是整套方案的经济学基础，比 Code-as-Action 本身更重要

Code-as-Action 只是手段，真正让账算得过来的是**一次推理、无限次零 Token 回放**。
ToolCall 路线每次回归都要重新付一遍模型钱，规模化必然崩；固化之后单用例 1$ 只付一次，
之后的 12.3w 次执行都是普通脚本在跑。

推论：**任何 GUI Agent 方案，先问"产物能不能沉淀成不含模型的资产"**。
如果答案是否，那它只能做探索工具，做不成回归基础设施。

### 3.2 三层架构的本质是"把不确定性关进中间层"

意图层人写、可确定；代码层固化、可确定；只有探索层允许不确定，而且被两条硬约束夹住
（不许改意图 + 确定性 >95% 才准固化）。这比"提高模型准确率"务实得多——
**不确定性不可消除，但可以被隔离和限流。**

同一个思路在知识库里又出现了一次：图谱写入先进暂存区、周 Review 后合并；
知识库分支只在需求上线时才回流 master。**每个自动化环节都配了一道人工闸门，
而且闸门都放在"影响范围会扩大"的那个点上**，不是均匀撒在流程里。

### 3.3 模型分层是成本设计，不是性能优化

主 Agent（长上下文、编码、泛化）+ 视觉理解 Agent（小上下文、低成本、确定）的拆分，
关键不在于快，而在于**把最贵的模型调用次数压到最低**：视觉识别是高频动作，
一个用例可能调几十次；计划生成是低频动作，一个需求调几次。频次和单价必须反向匹配。

### 3.4 断言失败的"竹子" case 暴露了一个方法论问题

页面标题写"竹子"、配图是玉簪，模型判断"不符"。它其实没错——是**用例的语义边界没定义清楚**：
该断言标题文案，还是断言图文一致性？

这说明视觉断言不能写成"页面内容为竹子"这种自然语言，而必须写成
"标题文案等于'竹子'"或"配图与标题所述植物一致"。
**自然语言的表达力是优势，模糊性是代价**——意图层的写作规范（而不是模型能力）
才是断言可靠性的瓶颈。这也解释了为什么意图层必须人写 + 前置 review。

### 3.5 知识库最狠的一条设计是"主动清理"

"记忆太少重复踩坑，太多产生幻觉"——大多数团队做知识库只想到"怎么写进去"，
这里明确把**淘汰**做成机制：L0 有 200 行硬上限并自动淘汰过期内容，L1 有 2000 行上限。

上限逼出摘要质量。没有上限的知识库最终等于没有知识库，因为 Agent 检索不到重点。

### 3.6 路径图谱解决的是"Agent 不知道路怎么走"这个真问题

纯探索的 Agent 会在 App 里绕远、钻死胡同，因为它不知道"个人页可以从对话流头像进"。
图谱把**页面拓扑 + deeplink + 布局结构 + 核心动线**变成可召回的结构化知识，
本质上是给 Agent 装了一张地图，把探索问题降维成查表 + 局部搜索。

`confidence` 分档处理（≥80 / 60–79 / <60 / recalledPaths 空 / 文件不存在）
是**优雅降级**的教科书写法：不可用时降级而不阻塞，并在文档头说明原因。

### 3.7 "像做 AI Coding 一样做测试"不是类比，是架构选择

所有上下文都在仓库里、跟代码在一起；知识库走 Git 分支；skills/ configs/ memory/ 目录；
每目录一个 `Agents.md`。这套结构就是 Claude Code / Cursor 那类 Coding Agent 的原生工作方式。

选择它的收益是**复用整个 Coding Agent 生态的工程惯例**（版本控制、review、diff、分支合并），
而不用自己发明一套测试知识管理系统。

---

## 四、读后笔记：存疑、风险与文档没讲的部分

### 4.1 数据口径需要打问号

- **75% 自动化率**的分母是什么？如果分母是"已被判定为可自动化的用例"，那和 p16 说的
  70–80% 可自动化区间叠加后，对全量用例的真实覆盖大约是 0.75 × 0.75 ≈ 56%。
- **68% 兼容性 Bug 自动召回率**是相对"最终被发现的兼容性 bug"，还是相对"人工回归发现的"？
  漏掉的 32% 是什么性质的，文档没讲。
- **98% 执行稳定性**——是单步成功率还是整条用例成功率？如果是单步 98%，
  一条 20 步的用例通过率只有 0.98²⁰ ≈ 67%。这个差别决定方案可不可信，文档没澄清。
- **单用例 1$** 只算了首次生成，没提**自愈重试**的摊销成本。按 p30，失败会走
  错误分类 → 自愈重试 → 重新生成，这几轮的 Token 可能远超首次。
- p16 自己说"非常规缺陷占 bug 数的大多数"，而方案覆盖的是常规那 70–80%。
  那么**整套投入对最终线上质量的边际贡献到底多大**？文档用"提效"叙事绕过了这个问题。
  这不是否定价值（省下的人力可以投到那 5–10% 上），但**必须诚实标注**。

### 4.2 固化脚本的维护成本被轻描淡写了

固化产物里存着坐标（`AVATAR_X`、`TAB_X`）。UI 一改，600+ 个固化脚本一起失效。
文档说靠自愈重试和重新生成解决，但这意味着**每个版本都有一批脚本要重跑 LLM 生成**——
"零 Token 回归"只在 UI 不变的窗口内成立。
真实成本应该是"零 Token 回归 + 每版 N% 脚本的重生成开销"，N 是多少没披露。

**这其实是传统 UI 自动化那个结构性矛盾的换形**：只是把"人改 XPath"换成了"模型改坐标"，
矛盾没消失，只是变便宜了。值得做，但别当成解决了。

### 4.3 遵循性输给 ToolCall 这一条，代价没讲透

p29 承认 Code-as-Action 的遵循性依赖模型的 coding 能力。这意味着：
换模型可能整批用例的生成质量抖动；模型写出能跑但语义错的代码（假通过）比 ToolCall 更难发现。
**"假绿"的检出机制文档完全没提**——CI 里一堆固化脚本 PASS，怎么知道它们断言的是对的东西？

### 4.4 人工闸门的真实工时没算

前置 review 意图层、周 Review 图谱变更、知识库审核、`confidence <60` 的人工 review、
commit 人工审核（p19）——这些全是人的时间。
方案宣称把人从执行里解放出来，但**新增的审核负担有多大，一个需求要几个人时**，没有数据。
如果审核成本 ≈ 原执行成本，那只是把工作从"点手机"换成"看 diff"。

### 4.5 环境准备被标为"最难的坑"，但方案里最薄

登录、Mock、实验、DSL、降级、内测……p25 列了十几项，
这些恰恰是**最依赖内部基建、最不可迁移**的部分。
文档给的是能力清单，不是解法。**对想复刻这套方案的团队，这里才是真正的门槛**——
没有 RedMTC 那种云测平台和一整套可测性基建，前面所有设计都跑不起来。

### 4.6 缺失：失败归因的准确率

p30 的错误分类诊断（环境错误 / 执行错误 / 用例错误）是整个自愈闭环的枢纽——
分错了就会把真 bug 当环境问题重试掉。**这个分类器的准确率是多少？漏报真 bug 的比例？**
文档一个字没提，而这恰恰是"自动化测试能不能被信任"的命门。

### 4.7 时间线上有点可疑

Stage 3 标 2025-10，落地数据是 2026 年春节大促（约 2026-02），
中间只有 4 个月就做到 106 机型 × 428 场景 / 12.3w 次执行 / 600+ S0 常态化。
要么之前有大量 Stage 1/2 的存量资产在托底（很可能，p42 的"存量召回 100%"暗示了这点），
要么规模化时间被压缩表述了。**如果自己团队没有存量自动化资产，别按 4 个月排期。**

---

## 五、可迁移清单：哪些能直接搬，哪些不能

### 5.1 可以直接搬（与业务、规模、基建无关的通用设计）

| 设计 | 为什么通用 |
|---|---|
| 三层架构（意图 / 探索 / 代码）+ 三条约束 | 纯粹的关注点分离，任何规模都成立 |
| Code-as-Action + 固化成零 Token 回归 | 经济学结论，与业务无关 |
| 模型分层（贵模型思考 / 便宜模型看图） | 频次 × 单价的反向匹配是普适原则 |
| 知识库 L0/L1/L2 + 行数硬上限 + 主动淘汰 | 上下文窗口约束对所有人一样 |
| 知识库走 Git 分支、需求上线才回流 master | 防污染机制，越小的团队越需要 |
| `confidence` 分档 + 优雅降级（不阻塞） | 工程惯例 |
| 错误三分类（环境 / 执行 / 用例）→ 分流处置 | 自愈闭环的最小骨架 |
| "人定标准、Agent 执行探索" | 组织原则 |

### 5.2 需要改造后才能搬

- **路径图谱**：核心价值成立，但小团队没有"真实流量"这一路数据源，
  也养不起周 Review 的图谱维护同学。可退化为**手写的页面拓扑 md**（页面别名、入口、deeplink、核心动线），
  L1 概览层足够，不必做成可视化平台。
- **视觉定位三层兜底**：语义 + DOM + 视觉。如果只做单端（比如只有 Web 或只有 Android），
  DOM 层就能覆盖大部分，视觉层只做兜底，成本能降一个数量级。
- **skill 仓库**：先放 repo 里就好（文档自己也说"开发时先放 repo，后续再抽离"）。
  别一开始就做 Skill 包分发。

### 5.3 搬不了 / 不该搬

- **RedMTC 那样的云测平台 + 106 机型矩阵**：这是钱和基建，不是设计。
  没有它就别定"多机型兼容性自动召回"这种目标。
- **一整套可测性基建**（实验 Mock / Switch Mock / 抓包 Mock / DSL 配置 / 降级 / 内测）：
  这些是小红书客户端团队多年埋的桩。**没有可测性桩，Agent 连稳定的初始状态都构造不出来。**
  ⚠️ 这是复刻这套方案的**第一个真实门槛**，必须先评估，不能放到后期。
- **75% / 92% / 98% 这些数字当目标**：它们是有存量资产托底、有专职团队的结果。
  照抄目标只会做出为指标硬编码的东西——文档第三个坑说的正是这个。

### 5.4 如果我要落地，起步顺序（个人判断）

1. **先算账**：手上有多少条回归用例、执行一轮多少人时、其中多少是"稳定且高频"的。
   没有这个基线，后面所有提效都无法证明。
2. **先补可测性**：能不能一键把 App 拉到指定登录态 / 指定实验分组 / 指定页面。
   做不到就先做这个，别碰 Agent。
3. **先做单端 + 单业务线的意图层写作规范**（含断言写法规范，见 3.4 的"竹子"教训）。
4. **再做"探索一次 → 固化一份脚本 → 进 CI"这条最小闭环**，10 条 S0 用例就够验证。
5. **最后才做知识库和图谱**——它们是规模化的产物，10 条用例时用不上，
   过早做就是自娱自乐。

> 一句话：**这套方案的顺序不是从上往下抄架构图，而是从下往上补基建。**
